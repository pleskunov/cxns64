# Complex Numbers

A lightweight Rust library implementing 64-bit complex number type with C ABI compatibility.

## Usage

Add this to your `Cargo.toml`:

```toml
[dependencies]
cxns64 = "0.0.1"
```

## Compatibility

The `cxns64` crate is tested for rustc 1.92 and greater.
